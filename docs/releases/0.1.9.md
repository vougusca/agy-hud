# 0.1.9 验证记录

日期：2026-09-02。发布基线：`466f949`；实现提交：`2f90bbb`、`f294acb`。

## 行为与验证

- 费用先于既有目录、分支和配额信息降级；使用官方 `total_usd`，估算值加 `~`，无效数据不显示。
- 兼容 Pro / Ultra / Free 的短名称和 Google AI 前缀；未知套餐显示 `Plan ?`。
- 中文、emoji 和组合字符按显示列计算，并按字素簇截断。
- 端口提示缓存仅优化发现成本；校验失败不得阻断原有完整发现流程。配额刷新频率与同帧修正保持不变。
- 原始基线：89 项测试通过。TDD 分别观察到渲染与列宽 7 项失败、探测 3 项失败、边界 2 项失败，以及最后的回退/新 emoji 3 项失败，再完成最小修复。
- 最终 `npm test`：109 项通过、0 失败、0 跳过；Node.js 18.20.8 与 Node.js 24.15.0 均通过。
- `npm audit`：0 漏洞。
- 隔离的真实 loopback HTTP 测试：冷发现使用全量 ps、定向身份校验与 lsof；热路径只使用定向身份校验，仍收到变化后的新配额。缓存与提示文件权限均为 0600。
- 仅包含发布文件的暂存目录通过 `agy plugin validate`；bundle 在没有 node_modules 的目录中通过单行/双行渲染验证。各版本来源均为 0.1.9，依赖许可证已随包分发。

## 独立审查轨迹

所有轮次均为只读、已提交快照对比同一发布基线，不将 TDD 中间态送审。

- r1：OCR / gpt-5.6-sol。确认费用挤掉既有信息；估算标记建议一并采用。
- r2：OCR / gpt-5.6-sol。审查 11 个文件，确认 1 项 Medium：可选进程身份检查不应阻断完整发现。已用失败测试复现，并在 `f294acb` 修复。
- r3：整批原生审查分流；本机未找到 Codex 插件脚本，降级为 Codex CLI / gpt-5.6-sol / high / read-only。未发现本 diff 引入的可复现实质问题；独立执行了相关测试、布局与恢复路径检查、TypeScript 检查及 bundle 一致性检查。完整写文件测试由外层运行。

审查收敛，无待修复的本次阻塞项。发布工作流仍需在 GitHub 上完成 CI、tag/版本一致性验证及归档发布，才视为发版完成。
